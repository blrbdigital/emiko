# Emiko Golf — Site Map

## Overview
Premium custom golf head covers brand. Japanese-inspired streetwear-meets-golf aesthetic.
Logo: Leaping rabbit with Japanese text "バニー" (bunny) + "golf" script.

## Tech Stack
- **Platform**: WordPress + WooCommerce (theme ready for Hostinger)
- **Interim hosting**: blrb-connect static (nginx + SSL on VPS, port 3012)
- **Domain**: emiko.blrbdigital.com
- **Repo**: github.com/blrbdigital/emiko
- **Auto-deploy**: GitHub Actions on push to main

## Structure
```
emiko/
├── index.html          # Static version (interim, served by nginx)
├── style.css           # All styles (dark theme + full WooCommerce overrides)
├── functions.php       # WP + WooCommerce setup, wrapper overrides
├── woocommerce.php     # WooCommerce template wrapper
├── header.php          # Nav with Shop link + mobile hamburger
├── footer.php          # 4-col footer + lightbox
├── front-page.php      # Homepage (mirrors index.html)
├── index.php           # Blog fallback
├── page.php            # Generic page
├── 404.php             # "Lost in the Rough" 404
├── .blrb-connect.json  # Deploy config (port 3012)
├── .blrb/nginx_spa.conf
├── .github/workflows/deploy.yml
└── assets/
    ├── js/main.js      # Nav, mobile menu, scroll reveal, lightbox
    └── images/         # logo.jpg + product-1 through product-10.png
```

## Design System
- **Background**: #0a0a0a (near-black)
- **Text**: #f5f5f0 (warm white)
- **Accent text**: rgba(255,255,255,0.3-0.7) for hierarchy
- **Borders**: rgba(255,255,255,0.06-0.08)
- **Fonts**: Inter (Latin), Noto Sans JP (Japanese)
- **Logo treatment**: filter: invert(1) to make black logo white on dark bg

## Sections (front-page.php)
1. **Hero** — Full-screen, logo, tagline "Your Game. Your Cover.", Japanese subtitle, CTA
2. **Marquee** — Scrolling ticker: leather, embroidery, handcrafted, etc.
3. **About** — Split layout: product-4 image + brand story + stats
4. **Gallery** — Feature row (2 large) + 6-item grid with hover overlays
5. **Lucky Collection** — Featured showcase of 6 colorful covers
6. **Process** — 4-step: Consultation → Design → Craft → Deliver
7. **Collections** — 3 cards: Personal, Corporate, Tournament
8. **Testimonials** — 2 quote cards
9. **CTA** — Email + Instagram buttons

## Deploy
- **Target**: WordPress + WooCommerce on Hostinger managed hosting
- **Interim**: Static via blrb-connect (port 3012) until WP is set up on Hostinger
- WooCommerce support: theme declares wc support, product gallery zoom/lightbox/slider
- Custom WC wrappers in functions.php (dark theme, no sidebar, 3-col grid)
- Full WooCommerce CSS overrides in style.css (product cards, single product, cart, checkout, forms)

## Hostinger Setup Checklist
1. Create WordPress site on Hostinger panel
2. Point emiko.blrbdigital.com DNS to Hostinger IP
3. Upload theme to wp-content/themes/emiko/
4. Activate theme in WP Admin > Appearance > Themes
5. Install & activate WooCommerce plugin
6. Settings > Reading > Static front page
7. Add products via WooCommerce > Products

## Gotchas
- Logo is black-on-white JPG — uses CSS `filter: invert(1)` to appear white on dark bg
- All product images are PNG with white/transparent backgrounds
- product-4.png is 4.2MB (large) — may want to optimize before production
- Fonts loaded from Google Fonts CDN (Inter + Noto Sans JP)
